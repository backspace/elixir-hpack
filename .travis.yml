language: elixir

elixir:
  - 1.3.2

otp_release:
  - 18.2.1

cache:
  directories:
  - .local.plt
  - plt-cache

before_script:
  - ls -al
  - ls -a plt-cache
  - "[ -f plt-cache/test-local.plt ] || mix dialyzer.plt"

script:
  - mix test
  - mix dialyzer --check_plt
