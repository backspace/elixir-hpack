language: elixir

elixir:
  - 1.3.2

otp_release:
  - 18.2.1

cache:
  directories:
  - plt-cache

before_script:
  - ls -a
  - "[ -f plt-cache/test-local.plt ] || mix dialyzer.plt"

script:
  - mix test
  - mix dialyzer --check_plt
